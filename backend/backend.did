type Result = 
 variant {
   err: text;
   ok: nat;
 };
type Post = 
 record {
   content: text;
   date: text;
   id: nat;
   title: text;
 };
service : {
  createPost: (text, text, text) -> (Result);
  getPost: (nat) -> (opt Post) query;
  getPosts: () -> (vec Post) query;
}
